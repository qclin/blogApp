<!DOCTYPE html> 
<head>
	<style>
		h1{
			font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
			color: #60AFFE;
		}
		body{ 
			text-align: left; 
			font-size: 20px;
			font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
			color: #60AFFE;
  			line-height: 1;
  			letter-spacing: 0px;
  			font-size: 30px;
  			font-weight: lighter;
		}
		h3{
			padding:0;
			margin:0;
		}
		a{
			text-align: right; 
			font-size: 20px;
			text-decoration: none;
		}
		div{
			margin:0;
			display: inline-block;
			padding: 0px 10px 10px 10px;
		}
	</style>
</head>

	<body> 
		<h1> blog on blogs on blogging </h1> 
		
			<% Object.keys(entries).forEach(function(key){ %>
			<div class = "list">
				<h3> <%= entries[key].title %> </h3><br> 
				<img src = "<%= entries[key].giphy %>" width = "auto" height="300px"><br>
				<a href = "/entries/<%= entries[key].id %>"> more </a>
			</div>
			<% }); %>


		<br><a href ="/entries/new"> write new post </a>
	</body>
	</html>
